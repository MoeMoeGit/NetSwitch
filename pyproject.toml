[project]
name = "netswitch"
version = "1.2.1"
description = "Windows 系统托盘网络配置切换工具"
requires-python = ">=3.12,<3.13"
dependencies = [
    "PyQt6",
]

[dependency-groups]
dev = [
    "pyinstaller",
    "Pillow",
]
